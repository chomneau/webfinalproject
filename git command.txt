1-go to project folder
2-copy url from github of the repository
3-run in project folder in commandline
    git remote // to check the remote branch
4-git add .
5-git status
6-git commit -m "message"
7- if you laptop or desktop don't have git username please confir
    git config --global user.name "your name"
    git config --global user.email "your email"

8-
git branch -M main
git push -u origin main

/////////

git push --all 
mean push all branch to remote server

git restore .
restore all the committed files back
